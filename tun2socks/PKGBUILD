# Maintainer: h3fang <hf.enigma@gmail.com>
pkgname=tun2socks
pkgver=2.3.1
pkgrel=1
pkgdesc="tun2socks - powered by gVisor TCP/IP stack."
arch=(x86_64)
url="https://github.com/xjasonlyu/tun2socks"
license=(GPL3)
makedepends=(go)
groups=(modified)
source=(
    "https://github.com/xjasonlyu/tun2socks/archive/refs/tags/v${pkgver}.tar.gz"
)
sha512sums=('d98da23129b269e685792eab6730f921423387569fae6fe83756ec20b41a0857733403cf9ae38b49e91606b94688aa17d4df0ba24ae0fd7792c5cb3094649e3b')

build() {
    cd "$srcdir/${pkgname}-${pkgver}"
    export GOPATH="${srcdir}/go"
    export LDFLAGS=
    export GOFLAGS="-buildmode=pie -trimpath -mod=readonly -modcacherw"
    make linux-amd64
}

package() {
    cd "$srcdir/${pkgname}-${pkgver}"
    install -Dm755 "build/tun2socks-linux-amd64" "$pkgdir/usr/bin/tun2socks"
}
