# Maintainer: h3fang hf.enigma#gmail

pkgname=pacfilter
pkgver=0.2.0
pkgrel=1
pkgdesc="Filter pacman log file"
url="https://github.com/h3fang/pacfilter"
license=('MIT')
groups=(modified)
makedepends=(cargo)
arch=('x86_64')
source=("https://github.com/h3fang/pacfilter/archive/refs/tags/v${pkgver}.tar.gz")

sha256sums=('35b714673f7215c6518fa494cbe2087f00cbd32c13edcd1ab4e79f3d0a170412')

build() {
    cd "$srcdir/$pkgname-$pkgver"
    cargo build --release
}

package() {
    cd "$srcdir/$pkgname-$pkgver"
    install -Dm755 "target/release/$pkgname" "$pkgdir/usr/bin/$pkgname"
}
